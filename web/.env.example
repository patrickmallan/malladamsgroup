# Web3Forms (https://web3forms.com): verify your email there; submissions route to that inbox.
# Vercel: Project → Settings → Environment Variables → add VITE_WEB3FORMS_ACCESS_KEY
# If unset, the site still shows a direct email path; submit shows an error with the mailto instead of sending.
VITE_WEB3FORMS_ACCESS_KEY=
